# Verified official agy fallback version for Termux installs.
#
# Default installs try the current official Linux ARM64 manifest first. If that
# path fails during an explicit install/update, the wrapper may retry this
# official versioned manifest once.
AGY_VERIFIED_FALLBACK_VERSION=1.0.2
