# Verified official agy fallback version for Termux runtime updates.
#
# The runtime tries the current official Linux ARM64 manifest first. For
# explicit install/update operations, it may retry this official versioned
# manifest once.
AGY_VERIFIED_FALLBACK_VERSION=1.0.2
